/** * HERO JOURNEY ΓÇö Learn ΓåÆ Solve ΓåÆ Build ΓåÆ Ship * Supports legacy .journey-visual and new .journey-steps vertical rail. * No auto-cycle, no continuous scroll handlers ΓÇö IntersectionObserver only. */(function () {  'use strict';  const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;  // --- Legacy horizontal journey (if present) ---  const journeyVisual = document.querySelector('.journey-visual');  const stages = document.querySelectorAll('.journey-stage');  const nodes = document.querySelectorAll('.journey-node');  if (journeyVisual && stages.length) {    function updateProgress(activeStage) {      journeyVisual.dataset.progress = activeStage;      nodes.forEach((node, i) => {        const n = i + 1;        node.classList.toggle('is-active', n === activeStage);        node.classList.toggle('is-complete', n < activeStage);      });    }    function activateStage(n) {      stages.forEach((s, i) => {        const num = i + 1;        s.classList.toggle('is-active', num === n);        s.classList.toggle('is-complete', num < n);      });      updateProgress(n);    }    function initLegacy() {      if (prefersReducedMotion) { stages.forEach(s => s.classList.add('is-complete')); activateStage(4); return; }      const obs = new IntersectionObserver((entries) => {        entries.forEach(e => { if (e.isIntersecting) activateStage(parseInt(e.target.dataset.stage, 10)); });      }, { rootMargin: '0px 0px -55% 0px', threshold: 0.15 });      stages.forEach(s => obs.observe(s));      stages.forEach((s, i) => {        s.setAttribute('tabindex', '0');        s.setAttribute('role', 'button');        s.addEventListener('mouseenter', () => activateStage(i + 1));        s.addEventListener('keydown', (e) => {          if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); activateStage(i + 1); }        });      });      activateStage(1);    }    if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', initLegacy);    else initLegacy();  }  // --- New vertical journey ---  const rail = document.querySelector('.journey-rail');  const steps = document.querySelectorAll('.journey-step');  const fill = document.getElementById('journeyFill');  if (rail && steps.length) {    function setActive(index) {      steps.forEach((s, i) => {        s.classList.toggle('is-active', i === index);        s.classList.toggle('is-complete', i < index);      });      if (fill) {        const pct = ((index + 1) / steps.length) * 100;        fill.style.height = pct + '%';      }    }    function initNew() {      if (prefersReducedMotion) {        steps.forEach(s => s.classList.add('is-complete'));        setActive(steps.length - 1);        if (fill) fill.style.height = '100%';        return;      }      const obs = new IntersectionObserver((entries) => {        entries.forEach(e => {          if (e.isIntersecting) {            const idx = parseInt(e.target.dataset.step, 10) - 1;            setActive(idx);          }        });      }, { rootMargin: '0px 0px -50% 0px', threshold: 0.2 });      steps.forEach(s => obs.observe(s));      steps.forEach((s, i) => {        s.setAttribute('tabindex', '0');        s.setAttribute('role', 'button');        s.setAttribute('aria-label', s.querySelector('h3') ? s.querySelector('h3').textContent : 'Step ' + (i + 1));        s.addEventListener('mouseenter', () => setActive(i));        s.addEventListener('focus', () => setActive(i));        s.addEventListener('keydown', (e) => {          if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); setActive(i); }          if (e.key === 'ArrowDown' && i < steps.length - 1) { e.preventDefault(); steps[i + 1].focus(); }          if (e.key === 'ArrowUp' && i > 0) { e.preventDefault(); steps[i - 1].focus(); }        });      });      setActive(0);    }    if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', initNew);    else initNew();  }})();